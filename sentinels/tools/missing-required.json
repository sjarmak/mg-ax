{
  "tools": [
    {
      "name": "search_code",
      "description": "Search for code in a repository.",
      "inputSchema": {
        "type": "object",
        "required": ["pattern", "repo"],
        "properties": {
          "pattern": {"type": "string"},
          "repo": {"type": "string"},
          "limit": {"type": "integer", "default": 50}
        }
      }
    }
  ]
}
